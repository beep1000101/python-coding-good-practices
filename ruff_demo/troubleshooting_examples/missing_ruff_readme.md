# Missing Ruff: Typical Error

If Ruff is not installed, you may see errors like:

```
Ruff: command not found
Cannot spawn ruff
```

To fix, install Ruff in your current environment:

```bash
pip install ruff
```

Make sure it's installed in the same Python environment that VS Code is using.
